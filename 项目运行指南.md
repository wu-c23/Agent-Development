# 网络小说智能推荐与舆情分析系统 — 运行指南

## 项目架构

```
                            ┌─────────────────────────────┐
                            │   System Integrator (苗文昊)   │
                            │   规范文档 / Data Contract     │
                            │   UID 规范 / OpenAPI 3.0      │
                            │   端口: (Gateway 待实现)       │
                            └──────────┬──────────────────┘
                                       │ UID 关联
        ┌──────────────┬───────────────┼───────────────┬──────────────┐
        ▼              ▼               ▼               ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│Trend Explorer│ │Safe-Search   │ │Sentiment     │
│  (吴隐)      │ │ Architect    │ │ Critic       │
│  端口 8001   │ │ (吴宸)       │ │ (武文杰)      │
│              │ │ 端口 8002    │ │ 端口 8003    │
│ 趋势数据采集  │ │ 语义搜索     │ │ 舆情分析     │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │               │               │
       └───────┬───────┴───────┬───────┘
               ▼               ▼
        ┌─────────────────────────────┐
        │     统一大屏 (Python 生成)    │
        │  📊 趋势大屏 + 📝 小说评论    │
        │  + 🤖 AI 智能推荐           │
        │  │
        └─────────────────────────────┘
```

| 模块 | 负责人 | API 端口 | 前端 | 状态 |
|------|--------|----------|------|------|
| System Integrator | 苗文昊 | — | — | 规范文档 |
| Trend Explorer | 吴隐 | **8001** | 统一大屏 (三合一) | ✅ |
| Safe-Search Architect | 吴宸 | **8002** | AI 对话 (集成到统一大屏) | ✅ |
| Sentiment Critic | 武文杰 | **8003** | 评论分析 (集成到统一大屏) | ✅ |

### Data Contract 端点

| 模块 | 端口 | Data Contract 端点 |
|------|------|-------------------|
| Trend Explorer | 8001 | `GET /api/v1/trend/hot` `GET /api/v1/trend/detail/{uid}` `GET /api/v1/trend/tags/evolution` |
| Safe-Search | 8002 | `POST /api/v1/search` `GET /api/v1/health` `POST /api/v1/books/index` |
| Sentiment Critic | 8003 | `GET /api/v1/sentiment/detail/{uid}` `GET /api/v1/sentiment/compare` `POST /api/v1/sentiment/refresh` |

---

## 一、环境要求

- **Windows 10/11**
- **Python 3.10+** (含 pip)
- **PowerShell 5.1+**
- 建议 8GB+ 内存

---

## 二、API Key 说明

### Mock 模式 (演示推荐 — 无需任何 Key)

**Mock 模式不需要配置任何 API Key**，所有模块均内置预置样例数据，可直接启动演示全部功能。

每个模块的 Mock Server 响应头均标注 `X-Mock: true`，方便区分。

### 真实模式：统一 DeepSeek API Key

**三个模块共用同一个 `DEEPSEEK_API_KEY`**，只需配置一次。

配置文件位置：**`Safe-Search Architect\.env`**（项目唯一 .env 文件）

| 环境变量 | 用途 | 默认值 |
|----------|------|--------|
| `DEEPSEEK_API_KEY` | DeepSeek API 密钥（必填） | — |
| `DEEPSEEK_BASE_URL` | API 地址 | `https://api.deepseek.com` |
| `DEEPSEEK_CHAT_MODEL` | 模型名 | `deepseek-v4-pro` |
| `LLM_TIMEOUT` | 调用超时（秒） | `180` |
| `LLM_RETRIES` | 重试次数 | `2` |
| `LLM_BATCH_DELAY` | 批量分析间隔（秒） | `8` |
| `NOVEL_TREND_LLM_TEMPERATURE` | 趋势分析温度 | `0.2` |
| `EMBEDDING_PROVIDER` | 向量嵌入方式 | `local`（免费） |
| `HF_ENDPOINT` | HuggingFace 镜像 | `https://hf-mirror.com` |

> 使用 DeepSeek API 的优势：一个 Key 同时支持 Chat Completions 和 Embeddings（OpenAI 兼容接口），无需分别申请多个平台的 Key。

### 配置方式

```powershell
# 进入 Safe-Search Architect 目录
cd "Safe-Search Architect"

# 从模板创建 .env
copy .env.example .env

# 用文本编辑器打开 .env，填入你的 DeepSeek API Key
notepad .env
```

**配置这一个文件**。Sentiment Critic 和 Trend Explorer 会自动从 `../Safe-Search Architect/.env` 读取配置。

**`.env` 文件已加入 `.gitignore`，不会被提交到 Git。**

**另外，你需要配置\Trend Explorer\trend-api-spec文件夹下的config.js,方法同上，需要加入api_key

> **演示建议**: 仅在需要演示真实 AI 分析、AI 推荐对话功能时才需配置 DeepSeek API Key。

---

## 三、安装依赖

打开 PowerShell，依次执行:

### 3.1 Sentiment Critic (舆情分析)

```powershell
cd "Sentiment Critic"
pip install -r requirements.txt
```

依赖: `fastapi>=0.109.0`, `uvicorn>=0.27.0`, `pydantic>=2.0`, `requests>=2.31`, `beautifulsoup4>=4.12`

### 3.2 Safe-Search Architect (语义搜索)

```powershell
cd "Safe-Search Architect"
pip install -r requirements.txt
```

依赖: `fastapi`, `uvicorn`, `chromadb`, `sentence-transformers`, `jieba`, `openai`, `rank-bm25` 等

### 3.3 Trend Explorer (趋势分析)

```powershell
cd "Trend Explorer"
pip install -r requirements.txt
```

此命令会一次性安装三个子模块的全部依赖（API 服务 + Scrapy 采集 + LangChain 分析）。各子模块原有独立 `requirements.txt` 仍然保留，也可单独安装。

依赖: `fastapi`, `uvicorn`, `pydantic`

### 3.4 统一大屏 (无需额外依赖)

统一大屏使用纯 Python 生成自包含 HTML 文件 (CDN 加载 ECharts)，集成三个视图：趋势大屏、小说评论、AI 推荐。无需 Node.js。依赖已包含在 3.3 的 `requirements.txt` 中。

---

## 四、数据集中存储说明

**所有书籍数据统一存储在 `Sentiment Critic/data/` 目录下**，三个模块通过 `sentiment_critic/data_store.py` 统一读写。

### 数据目录结构

```
Sentiment Critic/data/
├── novels.json              # 小说元数据索引 (1631+ 本)
├── sentiment_index.json      # 聚合舆情评分
├── reviews/                  # 评论数据 JSONL
│   ├── raw_reviews.jsonl     # 原始评论 (斗罗大陆等)
│   └── sample_reviews.jsonl  # 示例评论 (诡秘之主等)
└── trend_items/              # 榜单热度数据 (从爬虫导入)
    ├── trend_items_zongheng_*.jsonl
    └── ...
```

### 评论采集 (支持起点/豆瓣/贴吧/小红书)

```powershell
cd "Sentiment Critic"

# 单本采集 (默认起点+豆瓣)
python scripts/collect_reviews.py --book 诡秘之主
python scripts/collect_reviews.py --book 诡秘之主 --platform qidian    # 仅起点
python scripts/collect_reviews.py --book 诡秘之主 --platform douban    # 仅豆瓣

# 批量采集 — 自动从 novels.json 排行榜取 Top N
python scripts/batch_collect_reviews.py --top 10                        # 排行榜前10，起点+豆瓣
python scripts/batch_collect_reviews.py --top 20 --platform douban      # 前20，仅豆瓣
python scripts/batch_collect_reviews.py --top 5 --min-heat 500000       # 热度>50万的前5
```

### 数据刷新流程

```powershell
cd "Sentiment Critic"

# 1. 从爬虫导入榜单数据
python scripts/import_trend_data.py

# 2. 构建小说元数据索引
python scripts/build_novel_index.py

# 3. 批量采集评论（排行榜前10）
python scripts/batch_collect_reviews.py --top 10

# 4. 分析评论并构建舆情索引
python scripts/analyze_reviews.py --input data/runs/诡秘之主_douban_*.jsonl --book 诡秘之主
python scripts/build_sentiment_index.py

# 5. 一键刷新 API 缓存
# PowerShell: Invoke-WebRequest -Method POST http://127.0.0.1:8003/api/v1/sentiment/refresh
```

### Safe-Search 向量库同步

```powershell
# 从 novels.json 同步小说到向量搜索库
Invoke-WebRequest -Method POST http://127.0.0.1:8002/api/v1/books/index -Body '...'
```

---

## 五、小说评论查看 (已集成到统一大屏)

小说评论功能已集成到统一大屏的 **📝 小说评论** Tab 中，无需单独生成。

在统一大屏中：
- 点击排行榜中的小说名 → 自动切换到评论 Tab
- 或 AI 推荐结果中点击小说卡片 → 自动切换到评论 Tab

评论数据来自 Sentiment Critic 服务（端口 8003），包含综合评分、多维分析、优缺点和评论统计。

---

## 六、项目启动方式

如果已运行过数据采集，可以用真实 API 替代 Mock:

### 6.1 趋势 API (真实数据)

```powershell
cd "Trend Explorer\trend-api-spec"
uvicorn fastapi_demo:app --host 127.0.0.1 --port 8001 --reload
```

### 6.2 搜索 API (真实向量检索)

```
cd "Safe-Search Architect"
uvicorn src.safe_search.mock_server:app --host 127.0.0.1 --port 8002
```

### 6.3 舆情 API (真实采集)

```powershell
cd "Sentiment Critic"
uvicorn sentiment_critic.sentiment_api:app --host 127.0.0.1 --port 8003
```

### 6.4 启动趋势数据采集(这个部分最好不要运行，因为可能会有bug，相关数据已通过其他方式采集)

```powershell
cd "Trend Explorer\crawler-engine-v1.0"

# 安装采集依赖 (如未在根目录统一安装)
pip install -r requirements.txt
playwright install chromium

# Docker 方式 (推荐)
docker compose --profile manual run --build --rm crawler-zongheng

# 或本地 Python 方式
scrapy crawl zongheng_trends
```

---

## 七、前端界面说明

### 7.1 统一大屏 (unified_dashboard.html)

纯 Python 生成的自包含 HTML 文件，集成三个模块的全部前端功能。顶部 Tab 导航切换三个视图：

#### 📊 趋势大屏 Tab

复用 Trend Explorer 的 8 个交互面板（CDN ECharts 实现，无需 Node.js）：

| 组件 | 类型 | 说明 |
|------|------|------|
| KPI 指标卡片 | 卡片面板 | 细标签数、上升作品、监测平台、流派月份 |
| 热度曲线 | 折线图 | 标签热度多系列时序曲线 |
| 热词排行 | 柱状图 | 标签热度频率排序 |
| 词云 | 散点模拟 | 流派/标签文本权重可视化 |
| 平台对比 | 柱状图 | 各平台作品热度对比 |
| 流派迁移图 | 桑基图 | 流派间流动关系 |
| 迁移时间线 | 折线图 | 流派迁移强度随月份变化 |
| 上升作品表 | 数据表 | 小说名可点击 → 跳转到评论 Tab |

#### 📝 小说评论 Tab

通过 UID 跨模块关联，调用 Sentiment Critic API (端口 8003)：

- **综合评分**: 6 维评分卡片（综合/文笔/逻辑/人物/更新/毒性）
- **毒舌点评**: 一句话总结 + 优缺点列表
- **评论统计**: 好评率、差评率、各平台评论数分布
- **无数据时**: 显示"暂无评论数据"提示

#### 🤖 AI 推荐 Tab

调用 Safe-Search API (端口 8002) 进行语义搜索：

- **自然语言输入**: 用自然语言描述阅读偏好
- **避雷标签**: 可选标签过滤（后宫/烂尾/虐主/狗血/节奏慢）
- **推荐结果**: 小说名 + 相关度 + 匹配理由
- **小说名可点击**: 跳转到评论 Tab 查看详细分析
- **支持真实 DeepSeek API**: 配置 `DEEPSEEK_API_KEY` 后使用真实 LLM 进行向量检索、意图解析和风险评分

#### 🤖 角色扮演 Tab

可以自定义角色或使用提前设置好的角色，让AI扮演小说主角与其进行对话

### 7.2 Safe-Search Swagger UI (http://127.0.0.1:8002/docs)

FastAPI 自动生成的交互式 API 文档，可直接在浏览器中测试以下端点：

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/health` | 健康检查 + 索引统计 |
| `POST` | `/api/v1/search` | 语义搜索 + 避雷过滤 |
| `POST` | `/api/v1/books/index` | 批量添加/更新书籍索引 |
| `GET` | `/api/v1/books/{book_id}` | 查询单本书 + 实时风险评分 |
| `DELETE` | `/api/v1/books/{book_id}` | 从索引中删除书籍 |
| `GET` | `/api/v1/intent` | 意图解析（调试用） |

SafeSearch 模块还提供 MCP 服务（`python -m src.safe_search --mode mcp`），可在 Claude Code 中作为工具直接调用。

---

## 八、验证清单

启动所有服务后，依次验证:

```powershell
# 1. Trend Explorer 健康检查
curl http://127.0.0.1:8001/api/v1/trend/health

# 2. Trend Explorer 热门榜单
curl "http://127.0.0.1:8001/api/v1/trend/hot?limit=3"

# 3. Trend Explorer 单书详情 (诡秘之主)
curl http://127.0.0.1:8001/api/v1/trend/detail/987a2b45c6bd2baa73d750f13daf15b63c20145de2e85fff22ada96ad3d8b27b

# 4. Trend Explorer 标签演变
curl "http://127.0.0.1:8001/api/v1/trend/tags/evolution?tag=克苏鲁&period=30d"

# 5. Safe-Search 健康检查
curl http://127.0.0.1:8002/api/v1/health

# 6. Safe-Search 语义搜索
curl -X POST http://127.0.0.1:8002/api/v1/search `
  -H "Content-Type: application/json" `
  -d '{"query":"克苏鲁蒸汽朋克世界观严密","avoid_tags":["烂尾","后宫"],"top_k":5}'

# 7. Sentiment Critic 健康检查
curl http://127.0.0.1:8003/api/v1/sentiment/health

# 8. Sentiment Critic 单书舆情 (诡秘之主)
curl http://127.0.0.1:8003/api/v1/sentiment/detail/987a2b45c6bd2baa73d750f13daf15b63c20145de2e85fff22ada96ad3d8b27b

# 9. Sentiment Critic 多书对比
curl "http://127.0.0.1:8003/api/v1/sentiment/compare?uids=987a2b45c6bd2baa73d750f13daf15b63c20145de2e85fff22ada96ad3d8b27b,26d4613826eb8819a9a601cfb88eb7621b0e6cc5c46ffefa96b5dea22ca938dc"

# 10. 统一大屏已生成
ls trend-api-spec\unified_dashboard.html

# 11. 打开统一大屏，验证三个 Tab 功能:
#     - 趋势大屏: 图表正常渲染
#     - 小说评论: 点击排行榜小说名能跳转
#     - AI 推荐: 输入查询能返回推荐结果
```

全部返回正常 JSON 或 HTML 即表示系统运行正常。

---

## 九、常见问题

### Q: 端口被占用

```powershell
# 查看端口占用
netstat -ano | findstr "8001"
netstat -ano | findstr "8002"
netstat -ano | findstr "8003"

# 结束占用进程 (替换 PID)
taskkill /PID <PID> /F
```

### Q: Python 模块找不到

```powershell
# 确认在正确的目录下执行 pip install
cd "<模块目录>"
pip install -r requirements.txt

# 确认安装成功
pip list | findstr "fastapi uvicorn"
```

### Q: 趋势大屏打开后图表不显示或空白

1. 确认 `unified_dashboard.py` 运行成功，输出 `统一大屏已保存: ...`
2. 确认生成的 `unified_dashboard.html` 文件存在（约 38KB）
3. 检查网络是否可访问 CDN (cdn.jsdelivr.net)，ECharts 从 CDN 加载
4. 如果 API 不可用，大屏会自动使用内置 Mock 数据

### Q: 点击小说名后评论 Tab 显示"暂无评论数据"

1. 确认 Sentiment Critic API 是否运行: `curl http://127.0.0.1:8003/api/v1/sentiment/health`
2. 该小说的评论数据尚未采集。`novels.json` 已收录 1600+ 本小说元数据（来自爬虫榜单），但舆情评分仅覆盖已有评论的小说
3. 通过 `python scripts/collect_reviews.py --book <书名>` 采集评论后可补充数据

### Q: AI 推荐功能显示"服务不可用"

1. 确认 Safe-Search API 是否运行: `curl http://127.0.0.1:8002/api/v1/health`
2. Mock 模式: `uvicorn src.safe_search.mock_server:app --port 8002`
3. 真实模式 (需要 DeepSeek API Key): 先在 `Safe-Search Architect\.env` 中配置 `DEEPSEEK_API_KEY`，再启动 `uvicorn src.safe_search.api:app --port 8002`

### Q: ChromaDB / HuggingFace 模型下载失败 (真实模式)

Safe-Search 的真实向量模式需要从 HuggingFace 下载 `BAAI/bge-small-zh-v1.5` 模型 (~400MB)。首次启动可能需要几分钟。

如果网络受限：设置 `HF_ENDPOINT=https://hf-mirror.com` 使用国内镜像；或设置 `EMBEDDING_PROVIDER=api` 使用远程嵌入 API。引擎会自动降级为 BM25 关键词检索，搜索和避雷功能仍可正常使用。
