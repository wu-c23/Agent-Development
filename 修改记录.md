# 修改记录

## 1. 核心 Bug 修复: chunk_text 无限循环

**文件:** `Sentiment Critic/sentiment_critic/rag_engine.py`

**问题:** `chunk_text()` 函数在文本长度介于 `chunk_size`(600) 和 `chunk_size + overlap`(720) 之间时陷入无限循环。当 `end == len(text)` 且 `start = end - overlap` 仍然 `< len(text)` 时,`start` 无法前进,导致 KB 构建永远无法完成。

**修改:** 提取最后一个 chunk 后立即 `break`,不再计算下一个 start。

- 修改后 KB 构建时间: 0.6 秒 (之前: 超时 >120 秒)
- 同时修复了 KB 缓存从未生成的问题

---

## 2. 默认禁用 VectorStore

**文件:** `Sentiment Critic/sentiment_critic/rag_engine.py` (第 28 行)

**问题:** `sentence_transformers` 加载耗时 30s+ (Windows + Python 3.14),导致首次请求阻塞。

**修改:** 添加 `os.environ.setdefault("DISABLE_VECTOR_STORE", "true")`，默认禁用 VectorStore。TF-IDF 已足够当前场景使用。

---

## 3. 改进 LLM 诊断输出

**文件:** 
- `Sentiment Critic/sentiment_critic/rag_engine.py`
- `Sentiment Critic/sentiment_critic/sentiment_api.py`

**问题:** 在 uvicorn 下,`print()` 输出的诊断信息不可见(stdout 被捕获)。

**修改:** 所有关键诊断 `print` 改为 `print(..., file=sys.stderr, flush=True)`，确保在 uvicorn 下可见。

新增的 LLM 错误诊断:
- 添加 `import traceback` + `traceback.print_exc(file=sys.stderr)` 到所有 LLM 降级处理路径
- 服务器启动时输出: `DEEPSEEK_API_KEY` 状态、endpoint、模型名

---

## 4. 改进角色对话 Prompt

**文件:** `Sentiment Critic/sentiment_critic/rag_engine.py` (第 899 行)

**问题:** 角色 prompt 的规则 #9 限制回复为 "3-5 句话"，导致 LLM 回复过于简短。

**修改:** 改为"根据话题自然展开"，给 LLM 更多自由度。

---

## 5. 改进启发式回复 (LLM 不可用时的降级)

**文件:** `Sentiment Critic/sentiment_critic/rag_engine.py`

**问题:** 当 LLM 不可用时,降级回复非常模板化(仅 1 句)，体验差。

**修改:** 利用知识库检索结果(context)丰富回复内容,使无 LLM 时也能生成更有信息的回复。

---

## 6. 修复 AI 推荐 Tab 路由

**文件:** `Trend Explorer/trend-api-spec/unified_dashboard.py`

**问题:** AI 推荐 Tab (Tab 3) 调用 Safe-Search 语义搜索(端口 8002)的 `POST /api/v1/search/semantic`，这是纯搜索引擎,无论用户问什么都返回书籍搜索结果。

**修改:** 改为调用 Sentiment Critic (端口 8003) 的 `POST /api/v1/sentiment/chat`，使用 LLM 生成对话式回复。

- 用户问"你能做什么" → 描述功能(不再列出书籍)
- 用户请求推荐 → 知识库推荐 + LLM 说明
- 移除避雷标签 UI(隐藏)

**修复:** `result.answer.replace(\n/g, ...)` 中 `\n` 在 Python 模板字符串中被转义为换行符,导致生成的 HTML 中 JS 语法错误。改为 `\\n` 修复。

---

## 7. 修复 RAG 系统 Prompt

**文件:** `Sentiment Critic/sentiment_critic/rag_engine.py` (第 903 行)

**问题:** 系统 prompt 标题为 "网络小说智能推荐助手",导致 LLM 认为自己的唯一功能就是推荐书籍。

**修改:** 改为 "小说闲聊助手"，并添加规则 #7: 当用户问"你能做什么"时介绍功能而非推荐书籍。

---

## 8. 添加模式状态指示器

**文件:** `Trend Explorer/trend-api-spec/unified_dashboard.py`

**修改:** 在角色对话 Tab 添加 `charModeLabel` 指示器,根据服务器响应的 `method` 字段显示:
- `character_rag` → "🤖 LLM 模式 (DeepSeek AI 生成)"
- `mock` → "⚡ Mock 模式 (固定回复)"
- `character_heuristic` → "⚠️ 降级模式"

---

## 9. 更新项目运行指南

**文件:** `项目运行指南.md`

**修改:** 在第四节(Mock 模式)添加警告: Mock 模式下角色对话是固定模板回复,需要真实 AI 请改用真实 API。

---

## 如何启动

```powershell
# (1) 必需: 确保 API Key 已配置
#     文件: Safe-Search Architect/.env
#     DEEPSEEK_API_KEY=sk-xxx

# (2) 启动真实 API (端口 8003)
cd "Sentiment Critic"
PYTHONUNBUFFERED=1 uvicorn sentiment_critic.sentiment_api:app --port 8003 --host 0.0.0.0

# (3) 生成/刷新统一大屏
cd "Trend Explorer\trend-api-spec"
python unified_dashboard.py

# (4) 浏览器打开 unified_dashboard.html
```
